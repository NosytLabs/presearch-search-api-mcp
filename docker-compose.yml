version: '3.8'

services:
  presearch-mcp:
    build: .
    environment:
      - PRESEARCH_API_KEY=${PRESEARCH_API_KEY}
      - LOG_LEVEL=info
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
